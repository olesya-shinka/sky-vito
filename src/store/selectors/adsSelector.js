export const AdsSelector = (state) => state.ads.adsList;

//export const AdsSelectedSelector = (state) => state.ads.selectedAds;

export const AdsCommentsSelector = (state) => state.ads.adsComments;

export const selectorUpdate = (state) => state.ads.shouldUpdate;
